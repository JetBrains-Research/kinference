name: Tests

on:
  pull_request:

jobs:
  JVM:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'

  JS-IR:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
